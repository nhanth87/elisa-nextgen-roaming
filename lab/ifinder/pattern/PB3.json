{
  "pattern_id": "PB3",
  "pattern_name": "Invalid Reference",
  "validation_class": "semantic",
  "element": "An object reference derived from a message: an object identifier used as a key into an internal table, or a stored object pointer held by an asynchronous callback",
  "dangerous_operation": "Dereferencing or operating on the referenced object (table lookup or pointer use)",
  "missing_validation": "Validation that the referenced object exists, belongs to the current scope, and is still alive before use",
  "security_impact": "Use-after-free, null pointer dereference, or operations on the wrong entries -> DoS or other security impact",
  "pattern_description": "Protocol messages carry object identifiers used as keys into internal tables. Asynchronous callbacks may also store object pointers that become stale when freed. The code does not validate that referenced objects exist, belong to the current scope, and are still alive before use. A crafted message referencing a non-existent or cross-scope object, or a race condition where an object is freed while a callback holds its pointer, can cause Use-After-Free, null pointer dereference, or operations on wrong entries, leading to DoS or other security impact."
}
