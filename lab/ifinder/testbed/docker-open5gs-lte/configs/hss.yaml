# Open5GS HSS (4G EPC) — S6a Diameter to MME; subscriber store in MongoDB.
# Pinned to 10.100.2.8. db_uri points at the mongo container (10.100.2.2).
db_uri: mongodb://10.100.2.2/open5gs

global:
  max:
    ue: 1024

hss:
  freeDiameter: /open5gs/conf/hss.conf
