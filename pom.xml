<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>et.elisa</groupId>
  <artifactId>elisa-roaming</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Elisa Roaming — Unified SS7 + Diameter signaling platform (STP + IWF + DRA)</name>

  <!-- Dual-license (owner-held, Tran Nhan): AGPL-3.0-or-later (Community) |
       commercial (Operator). See docs/research/licensing-dual-model.md. -->
  <licenses>
    <license>
      <name>GNU Affero General Public License, Version 3.0 or later</name>
      <url>https://www.gnu.org/licenses/agpl-3.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <modules>
    <module>elisa-fabric</module>
    <module>elisa-signaling-core</module>
    <module>elisa-dra</module>
    <module>elisa-stp</module>
    <module>elisa-iwf</module>
    <module>bench</module>
    <module>lab/sas-diameter-testapp</module>
    <module>lab/sgsn-sim</module>
    <module>elisa-bom</module>
  </modules>

  <properties>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
    <quarkus.platform.version>3.37.3</quarkus.platform.version>
    <quarkus.build.skip>false</quarkus.build.skip>
    <quarkus.native.enabled>false</quarkus.native.enabled>
    <microjainslee.version>1.2.0-SNAPSHOT</microjainslee.version>
    <corsac.version>10.0.0-41-SNAPSHOT</corsac.version>
    <junit.version>5.10.2</junit.version>
    <log4j2.version>2.26.1</log4j2.version>
    <jackson.version>2.17.2</jackson.version>
    <micrometer.version>1.14.0</micrometer.version>
    <sctp.version>2.27.32</sctp.version>
    <!-- micro-jainslee ClusterManager + SIFS are compiled against ISPN 15. -->
    <infinispan.version>15.0.0.Final</infinispan.version>
    <jgroups.version>5.3.13.Final</jgroups.version>
    <protostream.version>5.0.1.Final</protostream.version>
    <surefire.version>3.5.4</surefire.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Declared BEFORE quarkus-bom so 2.26.1 wins over Quarkus's managed 2.26.0
           for every org.apache.logging.log4j:* artifact (api/core/bridges aligned). -->
      <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-bom</artifactId>
        <version>${log4j2.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.quarkus.platform</groupId>
        <artifactId>quarkus-bom</artifactId>
        <version>${quarkus.platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>et.elisa</groupId>
        <artifactId>elisa-fabric</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>et.elisa</groupId>
        <artifactId>elisa-dra</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.mobius-software.protocols.diameter</groupId>
        <artifactId>diameter-api</artifactId>
        <version>${corsac.version}</version>
      </dependency>
      <dependency>
        <groupId>com.mobius-software.protocols.diameter</groupId>
        <artifactId>diameter-impl</artifactId>
        <version>${corsac.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>${jackson.version}</version>
      </dependency>
      <dependency>
        <groupId>io.micrometer</groupId>
        <artifactId>micrometer-registry-prometheus</artifactId>
        <version>${micrometer.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mobicents.protocols.sctp</groupId>
        <artifactId>sctp-core</artifactId>
        <version>${sctp.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mobicents.protocols.sctp</groupId>
        <artifactId>sctp-impl</artifactId>
        <version>${sctp.version}</version>
      </dependency>
      <dependency>
        <groupId>org.mobicents.protocols.sctp</groupId>
        <artifactId>sctp-backend-fstack</artifactId>
        <version>${sctp.version}</version>
      </dependency>
      <dependency>
        <groupId>org.infinispan</groupId>
        <artifactId>infinispan-core</artifactId>
        <version>${infinispan.version}</version>
      </dependency>
      <dependency>
        <groupId>org.infinispan</groupId>
        <artifactId>infinispan-commons</artifactId>
        <version>${infinispan.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jgroups</groupId>
        <artifactId>jgroups</artifactId>
        <version>${jgroups.version}</version>
      </dependency>
      <dependency>
        <groupId>org.infinispan.protostream</groupId>
        <artifactId>protostream</artifactId>
        <version>${protostream.version}</version>
      </dependency>
      <dependency>
        <groupId>org.infinispan.protostream</groupId>
        <artifactId>protostream-types</artifactId>
        <version>${protostream.version}</version>
      </dependency>
      <dependency>
        <groupId>org.assertj</groupId>
        <artifactId>assertj-core</artifactId>
        <version>3.26.3</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire.version}</version>
          <configuration>
            <argLine>--add-modules jdk.incubator.vector -Djava.util.logging.manager=org.apache.logging.log4j.jul.LogManager -Dorg.jboss.logging.provider=log4j2</argLine>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-enforcer-plugin</artifactId>
          <version>3.5.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
        </plugin>
        <plugin>
          <groupId>${quarkus.platform.group-id}</groupId>
          <artifactId>quarkus-maven-plugin</artifactId>
          <version>${quarkus.platform.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>