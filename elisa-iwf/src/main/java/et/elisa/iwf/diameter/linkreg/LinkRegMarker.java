package et.elisa.iwf.diameter.linkreg;

/**
 * Empty marker package backing corsac registerApplication calls.
 * MUST stay free of other classes: the parser scans the whole package and
 * anonymous/nested classes (null canonical name) crash registration.
 */
public final class LinkRegMarker {

    private LinkRegMarker() {
    }
}
